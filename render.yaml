services:
  - type: web
    name: cops-weather-control
    runtime: docker
    plan: starter
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DASHBOARD_PASSWORD
        sync: false
      - key: BOLT_COOKIES
        sync: false
